services:
  ac-bridge:
    build: .
    container_name: ac-bridge-server
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ./config.json:/app/config.json
    environment:
      - LOG_LEVEL=INFO
